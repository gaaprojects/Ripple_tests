# secrets — NEVER commit (SPEC §0.3 rule 3)
.env
.env.*
!.env.example
*.seed
ops/config/*.local.yaml

# vendored sources (cloned per SPEC §0.1)
vendor/firefly/

# python
venv/
__pycache__/
*.pyc
services/risk/.venv/
services/risk/data/*.parquet
services/risk/registry/

# node
node_modules/
dist/
.next/
*.tsbuildinfo

# sqlite (local demo state) — including WAL/SHM sidecars
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
*.db
data/*.sqlite*

# next.js generated type shim
next-env.d.ts

# device simulator keyfile (holds the cold key — I2)
apps/bridge/.device-key.json

# cold treasury master seed — provisioning-only, transient (SPEC §7: never in .env)
ops/provisioning/.provision-secrets.json

# misc
.DS_Store
*.log

# claude code local settings (keep the vendored xrpl-dev skill committed)
.claude/settings.local.json
